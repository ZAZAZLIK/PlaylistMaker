<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/favoritesRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="@dimen/twelve"
        android:paddingEnd="@dimen/twelve"
        android:paddingTop="@dimen/twelve"
        android:clipToPadding="false"/>

    <LinearLayout
        android:id="@+id/favoritesPlaceholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <ImageView
            android:id="@+id/image_stub_favorites"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/mode"
            android:layout_marginBottom="24dp" />

        <TextView
            android:id="@+id/tvStubFavorites"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/stub_favorites"
            android:textSize="20sp"
            android:textColor="@color/textColorPrimary" />
    </LinearLayout>

</FrameLayout>
